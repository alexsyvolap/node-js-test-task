## Onix Internship Test Task

### Server setup

* copy `.env.example` to `.env`
* replace `SERVER_HOST` and `SERVER_PORT` with the ones you need
* install modules - `npm i`
* run server - `npm run serve`